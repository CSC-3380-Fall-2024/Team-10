var score_text = "Score: " + string(global.score);
var x_pos = room_width - 150; // Position near the left edge
var y_pos = 10; // Position near the top of the screen

draw_text(x_pos, y_pos, score_text);

