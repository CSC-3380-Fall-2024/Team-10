/**
 * Initializes the global key bindings for movement actions using specific keys.
 * 
 * @param global.key_up (integer) The global variable that stores the key binding for the "Up" action (mapped to "W").
 * @param global.key_down (integer) The global variable that stores the key binding for the "Down" action (mapped to "S").
 * @param global.key_left (integer) The global variable that stores the key binding for the "Left" action (mapped to "A").
 * @param global.key_right (integer) The global variable that stores the key binding for the "Right" action (mapped to "D").
 */
global.key_up = ord("W");
global.key_down = ord("S");
global.key_left = ord("A");
global.key_right = ord("D");

